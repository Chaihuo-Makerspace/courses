{
  "name": "@chaihuo/course-site",
  "version": "0.1.0",
  "private": true,
  "description": "柴火创客学院课程体系",
  "type": "module",
  "scripts": {
    "dev": "astro dev",
    "build": "astro build && cp .assetsignore dist/.assetsignore",
    "build:cf": "astro build && cp .assetsignore dist/.assetsignore",
    "build:node": "ADAPTER=node astro build",
    "start": "HOST=0.0.0.0 PORT=3000 node dist/server/entry.mjs",
    "preview": "astro preview --port 3000",
    "check": "astro check",
    "deploy": "wrangler deploy"
  },
  "dependencies": {
    "@astrojs/cloudflare": "^12.3.0",
    "@astrojs/node": "^9.5.2",
    "astro": "^5.0.0"
  },
  "devDependencies": {
    "@astrojs/check": "^0.9.0",
    "typescript": "^5.6.3",
    "wrangler": "^4.50.0"
  }
}