# Environment Variables Configuration Template
# Usage: Copy this file to .env and modify the values according to your deployment environment

# ============================================================================
# REQUIRED VARIABLES
# ============================================================================

# GitHub Repository Path (Required)
# Format: owner/repo-name
# Used by: deploy.sh to construct Docker image name (ghcr.io/${GITHUB_REPOSITORY}:${IMAGE_TAG})
# Example: seeed-studio/seeed-2026-course
GITHUB_REPOSITORY=seeed-studio/seeed-2026-course

# ============================================================================
# OPTIONAL VARIABLES
# ============================================================================

# Host Port Mapping (Optional, default: 3000)
# The port on which the application will be exposed on the host machine
# Valid range: 1-65535
# Common alternatives: 8080, 8000, 3001
PORT=3000

# Docker Image Tag (Optional, default: latest)
# Specifies which version of the Docker image to pull and run
# Common values: latest, main, v1.0.0, or specific commit SHA
# Used to construct full image name: ghcr.io/${GITHUB_REPOSITORY}:${IMAGE_TAG}
IMAGE_TAG=latest

# Node.js Runtime Environment (Optional, default: production)
# Controls application behavior and optimizations
# Valid values: development, production, staging
# development = verbose logging, source maps, hot reload
# production = optimized, minimal logging, compressed assets
NODE_ENV=production
