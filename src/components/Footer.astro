---
import { Icon } from "astro-icon/components";
const currentYear = new Date().getFullYear();

const locations = [
  {
    city: "深圳",
    tag: "HQ",
    address: "深圳市南山区万科云城设计社区 B622",
    email: "jiayu.wang@seeed.cc",
  },
  {
    city: "成都",
    tag: null,
    address: "成都市青羊区狮马路92号",
    email: "xia.huang@seeed.cc",
  },
];

const footerLinks = {
  about: {
    title: "关于我们",
    links: [
      { label: "学院简介", href: "/about" },
      { label: "师资团队", href: "/about#team" },
      { label: "合作伙伴", href: "/about#partners" },
    ],
  },
  social: {
    title: "关注我们",
    links: [
      { label: "抖音", href: "#" },
      { label: "小红书", href: "#" },
      { label: "X", href: "#" },
      { label: "Facebook", href: "#" },
      { label: "Instagram", href: "#" },
    ],
  },
};
---

<footer class="bg-gray-50 border-t border-gray-200">
  <div class="max-w-7xl mx-auto px-4 sm:px-6 lg:px-8">
    <!-- Main Footer Content -->
    <div
      class="py-12 grid grid-cols-1 md:grid-cols-2 lg:grid-cols-12 gap-y-12 gap-x-8 lg:gap-x-12"
    >
      <!-- Brand Column -->
      <div class="lg:col-span-4">
        <a href="/" class="flex items-center gap-3">
          <div
            class="w-10 h-10 bg-brand-yellow rounded-lg flex items-center justify-center"
          >
            <svg
              class="w-6 h-6 text-brand-black"
              viewBox="0 0 24 24"
              fill="none"
              stroke="currentColor"
              stroke-width="2"
              stroke-linecap="round"
              stroke-linejoin="round"
            >
              <path d="M12 2L2 7l10 5 10-5-10-5z"></path>
              <path d="M2 17l10 5 10-5"></path>
              <path d="M2 12l10 5 10-5"></path>
            </svg>
          </div>
          <span class="text-lg font-bold text-brand-black"
            >柴火创客 OPC 学院</span
          >
        </a>
        <p class="mt-4 text-sm text-gray-600 leading-relaxed">
          柴火创客 OPC 学院提供从硬件接入、边缘计算到 AI
          智能体的全链路技术交付能力，让每一个个体都能拥有“一个人也能上场”的全栈交付能力
        </p>
      </div>

      <!-- Location Cards - Center Column -->
      <div class="md:col-span-2 lg:col-span-5">
        <h4 class="text-sm font-semibold text-gray-900 mb-4">联系我们</h4>
        <div class="grid grid-cols-1 sm:grid-cols-2 gap-4">
          {
            locations.map((loc) => (
              <div>
                <div class="flex items-center gap-2 mb-2">
                  <Icon
                    name="lucide:map-pin"
                    class="w-4 h-4 text-brand-red shrink-0"
                  />

                  <span class="font-medium text-gray-900">{loc.city}</span>
                  {loc.tag && (
                    <span class="text-xs bg-brand-yellow text-brand-black px-1.5 py-0.5 rounded font-medium">
                      {loc.tag}
                    </span>
                  )}
                </div>
                <p class="text-sm text-gray-600 mb-2 pl-6">{loc.address}</p>
                <p class="text-sm text-gray-600 pl-6">{loc.email}</p>
              </div>
            ))
          }
        </div>
      </div>

      <!-- Links Column -->
      <div class="md:col-span-2 lg:col-span-3 grid grid-cols-2 gap-8">
        <!-- About -->
        <div>
          <h4 class="text-sm font-semibold text-gray-900 mb-4">
            {footerLinks.about.title}
          </h4>
          <ul class="space-y-2">
            {
              footerLinks.about.links.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-sm text-gray-600 hover:text-brand-black transition-colors"
                  >
                    {link.label}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>

        <!-- Social -->
        <div>
          <h4 class="text-sm font-semibold text-gray-900 mb-4">
            {footerLinks.social.title}
          </h4>
          <ul class="space-y-2">
            {
              footerLinks.social.links.map((link) => (
                <li>
                  <a
                    href={link.href}
                    class="text-sm text-gray-600 hover:text-brand-black transition-colors"
                  >
                    {link.label}
                  </a>
                </li>
              ))
            }
          </ul>
        </div>
      </div>
    </div>

    <!-- Bottom Bar -->
    <div class="py-6 border-t border-gray-200">
      <p class="text-sm text-gray-500 text-center">
        &copy; {currentYear} Chaihuo Makerspace OPC Academy. All rights reserved.
      </p>
    </div>
  </div>
</footer>

<style>
  .bg-brand-yellow {
    background-color: var(--color-brand-yellow, #f3d230);
  }
  .text-brand-black {
    color: var(--color-brand-black, #1a1a1a);
  }
  .text-brand-red {
    color: var(--color-brand-red, #d84144);
  }
</style>
