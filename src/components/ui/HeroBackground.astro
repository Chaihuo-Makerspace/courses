---

---

<div
    class="absolute inset-0 pointer-events-none overflow-hidden select-none"
    aria-hidden="true"
>
    <!-- Subtle Gradient Mesh -->
    <div
        class="absolute top-0 left-1/2 -translate-x-1/2 w-full h-full max-w-7xl opacity-30"
    >
        <div
            class="absolute top-10 left-10 w-96 h-96 bg-brand-yellow/10 rounded-full blur-3xl mix-blend-multiply filter"
        >
        </div>
        <div
            class="absolute top-0 right-10 w-96 h-96 bg-brand-red/10 rounded-full blur-3xl mix-blend-multiply filter"
        >
        </div>
    </div>

    <!-- Geometric Shapes -->
    <svg
        class="absolute inset-0 w-full h-full opacity-[0.03] text-gray-900"
        viewBox="0 0 100 100"
        preserveAspectRatio="none"
    >
        <pattern id="grid" width="8" height="8" patternUnits="userSpaceOnUse">
            <path
                d="M 8 0 L 0 0 0 8"
                fill="none"
                stroke="currentColor"
                stroke-width="0.5"></path>
        </pattern>
        <rect width="100%" height="100%" fill="url(#grid)"></rect>
    </svg>
</div>
